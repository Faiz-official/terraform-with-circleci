version: 2.1

jobs:
  terraform:
    docker:
      - image: hashicorp/terraform:1.6.6

    steps:
      - checkout

      - run:
          name: Terraform Init
          command: terraform init

      - run:
          name: Terraform Plan
          command: terraform plan -var-file="terraform.tfvars"

      - run:
          name: Terraform Apply
          command: terraform apply -auto-approve -var-file="terraform.tfvars"

workflows:
  terraform_pipeline:
    jobs:
      - terraform